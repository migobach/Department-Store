<h1><%= @category.name %> - <%= @item.name %></h1>
<hr />
<p><%= @item.description %></p>
<h4>Price: $<%= @item.price %></h4>


<%= link_to 'Edit Item', edit_category_item_path(@category, @item) %>
<%= link_to 'Delete Item', category_item_path(@category, @item), method: :delete %>

