<h1>Deparment Store</h1>
<p>Click on a deparment below to see inventory</p>
<%= link_to 'Add a Department', new_category_path %>

<% @categories.each do |category| %>
<ul>
  <li>
    <%= link_to category.name, category_items_path(category.id) %>
  </li>
</ul>
<% end %>
